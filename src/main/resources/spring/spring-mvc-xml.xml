<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd http://www.springframework.org/schema/context https://www.springframework.org/schema/context/spring-context.xsd http://www.springframework.org/schema/mvc https://www.springframework.org/schema/mvc/spring-mvc.xsd">
    <!-- 1. 只扫描 controllerxml 包：
         service/mapper/事务/AOP 全部留在根容器(spring-xml.xml)，避免父子容器重复注册同一 bean -->
    <context:component-scan base-package="com.wyy.controllerxml"/>

    <!-- 2. 开启 MVC 注解解析（@RequestMapping/@RequestParam/@ResponseBody 等），
         并注册 JSON 消息转换器（若引入 jackson-databind） -->
    <mvc:annotation-driven/>

    <!-- 3. 静态资源(/css/、/js/、/images/)交还 Tomcat default servlet 处理，
         AuthFilter 白名单里已放行这些前缀，鉴权行为不变 -->
    <!--    <mvc:default-servlet-handler/>-->

    <!-- 3.1 静态资源由 Spring MVC 自己处理，不再交给容器 DefaultServlet，下面按现有的 css/js/images 目录分别映射 -->
    <mvc:resources mapping="/css/**" location="/css/" cache-period="3600"/>
    <mvc:resources mapping="/js/**" location="/js/" cache-period="3600"/>
    <mvc:resources mapping="/images/**" location="/images/" cache-period="3600"/>


    <!-- 4. 纯 XML 解决首页：DispatcherServlet 映射 "/" 后，
         web.xml 的 welcome-file 机制失效（它由 DefaultServlet 负责），访问 / 会 404，
         用 view-controller 把 "/" 直接转发到 index.jsp -->
    <mvc:view-controller path="/" view-name="index"/>

    <!-- 5. JSP 视图解析：现有 JSP 都在 webapp 根目录，prefix 保持 / -->
    <bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value="/"/>
        <property name="suffix" value=".jsp"/>
    </bean>
</beans>